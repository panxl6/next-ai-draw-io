name: Deploy to Ubuntu Server

on:
  push:
    branches:
      - main  # 当推送到 main 分支时触发部署
  workflow_dispatch:  # 允许手动触发

# 并发控制：确保一次提交只部署一次
# 使用 github.ref 确保同一分支的多个提交不会同时部署
# cancel-in-progress: true 表示如果新的提交触发部署，会取消正在进行的旧部署
concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '24'
        cache: 'npm'

    - name: Install dependencies
      run: |
        npm ci

    - name: Build application
      run: |
        npm run build
      env:
        NODE_ENV: production
        NEXT_TELEMETRY_DISABLED: 1

    - name: Prepare deployment files
      run: |
        mkdir -p deploy-package
        # Verify standalone build exists
        if [ ! -d ".next/standalone" ]; then
          echo "❌ 错误: 未找到 standalone 构建文件"
          echo "请确保 next.config.ts 中设置了 output: 'standalone'"
          exit 1
        fi
        # Copy standalone build output (preserve directory structure)
        echo "📦 复制 standalone 构建文件"
        echo "检查源文件:"
        ls -la .next/ | head -10
        echo ""
        echo "检查 standalone 目录:"
        ls -la .next/standalone/ | head -10
        echo ""
        mkdir -p deploy-package/.next
        echo "复制 standalone 目录..."
        cp -rv .next/standalone deploy-package/.next/ 2>&1 | head -20
        echo ""
        mkdir -p deploy-package/.next/static
        echo "复制 static 目录..."
        cp -rv .next/static/* deploy-package/.next/static/ 2>&1 | head -20 || true
        echo ""
        # Verify copy result
        echo "验证复制结果:"
        if [ ! -d "deploy-package/.next/standalone" ]; then
          echo "❌ 错误: deploy-package/.next/standalone 目录复制失败"
          exit 1
        fi
        if [ ! -f "deploy-package/.next/standalone/server.js" ]; then
          echo "❌ 错误: deploy-package/.next/standalone/server.js 文件不存在"
          exit 1
        fi
        echo "✅ standalone 文件复制成功"
        echo "deploy-package/.next 目录内容:"
        ls -la deploy-package/.next/
        echo ""
        echo "deploy-package/.next/standalone 目录内容（前10项）:"
        ls -la deploy-package/.next/standalone/ | head -10
        echo ""
        # Copy public assets
        cp -r public deploy-package/
        # Copy deployment scripts
        cp deploy.sh deploy-package/
        cp next-ai-draw-io.service deploy-package/
        # Copy environment file if exists
        if [ -f ".env.production" ]; then
          echo "📋 复制 .env.production 文件"
          cp .env.production deploy-package/
        else
          echo "⚠️  警告: .env.production 文件不存在，将跳过复制"
        fi

    - name: Validate deployment package
      run: |
        echo "🔍 验证部署包结构..."
        # Check root directory structure
        if [ ! -d "deploy-package/.next" ]; then
          echo "❌ 错误: deploy-package/.next 目录不存在"
          exit 1
        fi
        if [ ! -d "deploy-package/public" ]; then
          echo "❌ 错误: deploy-package/public 目录不存在"
          exit 1
        fi
        if [ ! -f "deploy-package/deploy.sh" ]; then
          echo "❌ 错误: deploy-package/deploy.sh 文件不存在"
          exit 1
        fi
        if [ ! -f "deploy-package/next-ai-draw-io.service" ]; then
          echo "❌ 错误: deploy-package/next-ai-draw-io.service 文件不存在"
          exit 1
        fi
        # Check .next/standalone directory
        if [ ! -d "deploy-package/.next/standalone" ]; then
          echo "❌ 错误: deploy-package/.next/standalone 目录不存在"
          exit 1
        fi
        if [ ! -f "deploy-package/.next/standalone/server.js" ]; then
          echo "❌ 错误: deploy-package/.next/standalone/server.js 文件不存在"
          exit 1
        fi
        # Check .next/static directory
        if [ ! -d "deploy-package/.next/static" ]; then
          echo "⚠️  警告: deploy-package/.next/static 目录不存在（可能为空）"
        fi
        # Check .env.production file (optional)
        if [ -f "deploy-package/.env.production" ]; then
          echo "✅ deploy-package/.env.production 文件存在"
        else
          echo "⚠️  警告: deploy-package/.env.production 文件不存在（可选）"
        fi
        # Display deployment package structure
        echo "✅ 部署包结构验证通过"
        echo "📁 部署包内容:"
        ls -la deploy-package/
        echo ""
        echo "📁 .next 目录内容:"
        ls -la deploy-package/.next/
        echo ""
        echo "📁 .next/standalone 目录内容（前10项）:"
        ls -la deploy-package/.next/standalone/ | head -10
        echo ""
        echo "📁 .next/static 目录内容（前10项）:"
        ls -la deploy-package/.next/static/ | head -10 || echo "（目录为空或不存在）"

    - name: Create deployment archive
      run: |
        echo "📦 创建部署压缩包..."
        cd deploy-package
        tar -czf ../deploy-package.tar.gz .
        cd ..
        echo "✅ 压缩包创建完成"
        ls -lh deploy-package.tar.gz
        echo ""
        echo "验证压缩包内容:"
        tar -tzf deploy-package.tar.gz | head -20 || true

    - name: Deploy to server
      uses: appleboy/scp-action@v0.1.7
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        port: ${{ secrets.SERVER_PORT || 22 }}
        source: "deploy-package.tar.gz"
        target: "/tmp"
        timeout: 30s
        command_timeout: 10m
        debug: true

    - name: Extract deployment files on server
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        port: ${{ secrets.SERVER_PORT || 22 }}
        timeout: 30s
        command_timeout: 10m
        debug: true
        script: |
          echo "📦 解压部署文件..."
          mkdir -p /tmp/deploy-next-ai-draw-io
          cd /tmp/deploy-next-ai-draw-io
          tar -xzf /tmp/deploy-package.tar.gz
          rm -f /tmp/deploy-package.tar.gz
          echo "✅ 解压完成"
          echo ""
          echo "验证解压后的文件:"
          ls -la /tmp/deploy-next-ai-draw-io/
          echo ""
          if [ -d "/tmp/deploy-next-ai-draw-io/.next" ]; then
            echo "✅ .next 目录存在"
            ls -la /tmp/deploy-next-ai-draw-io/.next/
          else
            echo "❌ 错误: .next 目录不存在"
            exit 1
          fi

    - name: Verify deployment files
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        port: ${{ secrets.SERVER_PORT || 22 }}
        timeout: 30s
        command_timeout: 10m
        debug: true
        script: |
          echo "检查部署文件:"
          ls -la /tmp/deploy-next-ai-draw-io/
          if [ -d "/tmp/deploy-next-ai-draw-io/.next" ]; then
            echo ".next 目录存在:"
            ls -la /tmp/deploy-next-ai-draw-io/.next/
            if [ -d "/tmp/deploy-next-ai-draw-io/.next/standalone" ]; then
              echo ".next/standalone 目录存在:"
              ls -la /tmp/deploy-next-ai-draw-io/.next/standalone/ | head -10
            else
              echo "错误: .next/standalone 目录不存在"
              exit 1
            fi
          else
            echo "错误: .next 目录不存在"
            exit 1
          fi

    - name: Execute deployment script
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        port: ${{ secrets.SERVER_PORT || 22 }}
        timeout: 30s
        command_timeout: 10m
        debug: true
        script: |
          chmod +x /tmp/deploy-next-ai-draw-io/deploy.sh
          sudo /tmp/deploy-next-ai-draw-io/deploy.sh

